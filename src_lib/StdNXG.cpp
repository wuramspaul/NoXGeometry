#include "StdNXG.hpp"

tree_t* ObjSearch(TreeObj_t* root){
  
  int min = ((TreeMetd_t*)root->Mets[0]->Stru)->dif;

}

tree_t* BGTMain(tree_t* root){
  if(root->type == 0){

  } 
  if(root->type == 1){
    
  } 
  return root;
}