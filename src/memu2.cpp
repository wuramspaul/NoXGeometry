#include <iostream>
#include <ncurses.h>
#define MAXNAME 255
struct inputmenu{
  bool create;
  bool save;
  bool load;
  bool exit;
  bool retter;
};

struct outputmes{
  char create[MAXNAME];
  
};

int main() {
  return 0;
}